import styled from 'styled-components'

const BadgeBox = styled.div`
  border-radius: var(--border-radius-medium);
  width: fit-content;
  font-size: 14px;
  padding: 0.8rem 1.6rem;
  background-color: var(--color-primary-medium);
  color: var(--f-dark-white);
`

export { BadgeBox }
